// import React, {useState} from 'react';

// export const ShowHide = (component, visibile=false) => {
//     const [visibleContent, setVisibleContent] = useState(() => visibile)
//     return [
//         visibleContent ? component : null,
//         () => setVisibleContent((content) => !content)
//     ]
// }

// export default ShowHide
